query MeetupsQuery($user_id: Int!) {
    meetups(user_id: $user_id) {
        data {
            id
            title
            introduction
            user {
                id
            }
            phone
            wechat
        }
    }
}
