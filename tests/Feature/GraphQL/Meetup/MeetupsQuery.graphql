# 我创建的约单列表
query meetupsQuery($user_id:Int!){
    meetups(user_id:$user_id){
        data{
            id
            title
            description
            participants{
                data{
                    id
                    avatar
                    name
                }
            }
            address
            expires_at
        }
    }
}
