extend type Mutation {
    """
    发私信
    """
    createMeetup(
        introduction: String
        phone: String
        images: [String!]
    ): Meetup @field(resolver: "App\\Meetup@resolveCreateMeetup")
}
