extend type Mutation @guard {
    createChat(
        """
        参与聊天的用户的ids
        """
        uids: [Int!]
        """
        参与聊天的用户的ids(答赚版) 
        废弃(请用统一的uids)
        """
        users: [Int!]
    ): Chat! @field(resolver: "App\\Chat@resolveCreateChat")
}
